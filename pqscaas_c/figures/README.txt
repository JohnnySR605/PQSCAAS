This directory holds plotted figures (PNG).

It is INTENTIONALLY EMPTY in the source distribution.

After `scripts/run_all.sh` has populated `results/*.csv`, run:
    python3 plot_scripts/plot_all.py
to regenerate all 12 figures from your own measured data.

Never paste figures generated by an AI assistant into a paper.
